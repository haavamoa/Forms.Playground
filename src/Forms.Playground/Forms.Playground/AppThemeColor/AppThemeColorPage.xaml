<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Forms.Playground.AppThemeColor.AppThemeColorPage">
    <!-- New way-->
    <Grid BackgroundColor="{OnAppTheme Light={StaticResource LightGridColor}, Dark={StaticResource DarkGridColor}}">
        <Label HorizontalOptions="Center"
               VerticalOptions="Center"
               Text=".NET"
               FontSize="96"
               FontAttributes="Bold"
               TextColor="{OnAppTheme Light={StaticResource LightLabelTextColor}, Dark={StaticResource DarkLabelTextColor}}" />
    </Grid>

    <!-- Old way -->
    <!--<Grid BackgroundColor="{DynamicResource DynamicGridColor}">
        <Label HorizontalOptions="Center"
               VerticalOptions="Center"
               Text=".NET"
               FontSize="96"
               FontAttributes="Bold"
               TextColor="{DynamicResource DynamicLabelTextColor}" />
    </Grid>-->

</ContentPage>